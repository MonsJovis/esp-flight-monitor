/* Measures every place name in the table against the hero ladder, on the device,
 * with the real faces. Fills PLAN.md's "longest destination name that fits at
 * 100 px" and validates the 100 -> 76 -> 56 shrink ladder against actual data
 * rather than an estimate. */
#pragma once
void dbg_metrics_hero(void);
